[pytest]
markers =
    online: live-network test against real golden URLs (run with --online)
    e2e: spawns the mock-source-site Vite server on port 5174 (needs Node + npm install)
    playwright: Playwright browser automation tests against the Vite dev server
